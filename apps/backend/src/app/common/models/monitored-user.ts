/**
 * Interface representing a user being monitored in the system.
 * @property {number} [id] - Optional unique identifier for the monitored user.
 */
export interface MonitoredUser {
  id?: number;
}
