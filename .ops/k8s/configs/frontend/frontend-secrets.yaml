apiVersion: v1
kind: Secret
metadata:
  name: frontend-secrets
type: Opaque
stringData:
  KEYCLOAK_URL: '${KEYCLOAK_EXTERNAL_URL}'
  KEYCLOAK_REALM: '${KEYCLOAK_REALM}'
  KEYCLOAK_CLIENT_ID: '${KEYCLOAK_CLIENT_UI}'
